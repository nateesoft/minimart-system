# 🚀 คำแนะนำการติดตั้งและใช้งาน Minimart POS

## 📋 ขั้นตอนการติดตั้งแบบละเอียด

### 1. ตรวจสอบข้อกำหนดของระบบ

ก่อนเริ่มต้น กรุณาตรวจสอบว่าคุณมีซอฟต์แวร์เหล่านี้ติดตั้งอยู่แล้ว:

- **Node.js** เวอร์ชัน 18.17 หรือสูงกว่า
  - ตรวจสอบ: `node --version`
  - ดาวน์โหลด: https://nodejs.org

- **npm** (มากับ Node.js) หรือ **yarn** หรือ **pnpm**
  - ตรวจสอบ: `npm --version`

### 2. เปิดโฟลเดอร์โปรเจค

```bash
cd minimart-pos-app
```

### 3. ติดตั้ง Dependencies

เลือกใช้ตัวใดตัวหนึ่ง:

**ใช้ npm:**
```bash
npm install
```

**ใช้ yarn:**
```bash
yarn install
```

**ใช้ pnpm:**
```bash
pnpm install
```

> ⏱️ การติดตั้งอาจใช้เวลา 2-5 นาทีขึ้นอยู่กับความเร็วอินเทอร์เน็ต

### 4. รันโปรเจค

**Development Mode:**
```bash
npm run dev
# หรือ
yarn dev
# หรือ
pnpm dev
```

### 5. เปิดเบราว์เซอร์

เปิดเบราว์เซอร์และไปที่:
```
http://localhost:3000
```

## 🎯 การใช้งานระบบ POS

### หน้าแรก (Home Page)
1. เปิด `http://localhost:3000`
2. คุณจะเห็นหน้าแนะนำระบบพร้อมฟีเจอร์ต่างๆ
3. คลิกปุ่ม **"เริ่มต้นใช้งานระบบ POS"**

### หน้าระบบขายหน้าร้าน (POS Page)

#### 🔍 การสแกนบาร์โค้ด
1. กดปุ่ม **F2** เพื่อโฟกัสที่ช่องสแกนบาร์โค้ด
2. พิมพ์หมายเลขบาร์โค้ด (13 หลัก) หรือสแกนด้วยเครื่องสแกนบาร์โค้ด
3. กด Enter หรือคลิกปุ่ม "สแกน"
4. สินค้าจะถูกเพิ่มเข้าตะกร้าโดยอัตโนมัติ

**ตัวอย่างบาร์โค้ดสินค้า:**
- น้ำดื่ม: `8851234560001`
- โค้ก: `8851234560002`
- มาม่า: `8851234560007`
- ไข่ไก่: `8851234560020`

#### 🔎 การค้นหาสินค้า
1. พิมพ์ชื่อสินค้าในช่องค้นหา
2. ระบบจะกรองสินค้าแบบเรียลไทม์
3. คลิกที่สินค้าที่ต้องการเพื่อเพิ่มในตะกร้า

#### 📑 การเลือกหมวดหมู่
- คลิกที่แท็บหมวดหมู่: **ทั้งหมด**, **เครื่องดื่ม**, **ขนม**, **อาหาร**, **ของใช้**
- ระบบจะแสดงเฉพาะสินค้าในหมวดหมู่นั้น

#### 🛒 การจัดการตะกร้าสินค้า
1. **เพิ่มจำนวน**: คลิกปุ่ม **+** ในตะกร้า
2. **ลดจำนวน**: คลิกปุ่ม **-** ในตะกร้า
3. **ลบสินค้า**: คลิกไอคอนถังขยะ 🗑️
4. **ล้างทั้งหมด**: คลิกปุ่ม "ล้างรายการ"

#### 💰 การชำระเงิน
1. คลิกปุ่ม **"ชำระเงิน"** สีเขียว
2. ระบบจะแสดงหน้าต่างชำระเงิน
3. **พิมพ์จำนวนเงินที่รับ** หรือคลิกปุ่มจำนวนเงินด่วน
4. ระบบจะแสดงเงินทอนอัตโนมัติ
5. คลิก **"ยืนยันการชำระเงิน"** หรือ **"ชำระด้วยบัตร"**
6. ระบบจะล้างตะกร้าและพร้อมสำหรับลูกค้าคนถัดไป

## 🎨 ฟีเจอร์พิเศษ

### ระบบแจ้งเตือนสต็อก
- 🟢 **สต็อกปกติ**: ไม่มีเครื่องหมาย
- 🟠 **สต็อกเหลือน้อย** (≤10 ชิ้น): แสดงป้าย "เหลือน้อย"
- 🔴 **สินค้าหมด** (0 ชิ้น): แสดงป้าย "หมด" และไม่สามารถเพิ่มในตะกร้าได้

### การป้องกันข้อผิดพลาด
- ❌ ไม่สามารถเพิ่มสินค้าเกินสต็อกที่มี
- ❌ ไม่สามารถชำระเงินถ้าเงินไม่พอ
- ✅ แสดงข้อความแจ้งเตือนที่ชัดเจน

### เสียงแจ้งเตือน
- 🔊 มีเสียง beep เมื่อสแกนบาร์โค้ดสำเร็จ

## 🔧 การปรับแต่งและพัฒนาต่อ

### เปลี่ยนข้อมูลสินค้า
แก้ไขไฟล์: `data/products.ts`

```typescript
// เพิ่มสินค้าใหม่
{
  id: 31, // ต้องไม่ซ้ำกับสินค้าอื่น
  name: 'ขนมปังโฮลวีท',
  category: 'food',
  price: 42,
  barcode: '8851234560031', // 13 หลัก
  stock: 50,
  image: '🍞',
  unit: 'ถุง'
}
```

### เพิ่มหมวดหมู่ใหม่
แก้ไขไฟล์: `data/products.ts`

```typescript
// เพิ่มใน categories array
{
  id: 'frozen',
  name: 'อาหารแช่แข็ง',
  icon: '🧊'
}
```

### เพิ่มระบบส่วนลด
แก้ไขไฟล์: `components/minimart-pos.tsx`

```typescript
// หาบรรทัดนี้และเปลี่ยน
const discount = 0; 
// เป็น
const discount = subtotal * 0.1; // ส่วนลด 10%
```

## 📊 Build สำหรับใช้งานจริง (Production)

เมื่อพร้อมใช้งานจริง:

```bash
# Build โปรเจค
npm run build

# รัน Production Server
npm start
```

เว็บจะรันที่: `http://localhost:3000`

## 🌐 Deploy ไปยัง Cloud

### Vercel (แนะนำ)
1. ติดตั้ง Vercel CLI: `npm i -g vercel`
2. รัน: `vercel`
3. ทำตามคำแนะนำ

### Netlify
1. Build: `npm run build`
2. Upload โฟลเดอร์ `.next` และ `public`

### Docker
สร้างไฟล์ `Dockerfile`:
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

## ❓ แก้ไขปัญหาทั่วไป

### ปัญหา: Port 3000 ถูกใช้งานอยู่
**วิธีแก้:**
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID [PID_NUMBER] /F

# Mac/Linux
lsof -ti:3000 | xargs kill -9
```

### ปัญหา: npm install ช้าหรือ error
**วิธีแก้:**
```bash
# ลบ node_modules และ package-lock.json
rm -rf node_modules package-lock.json

# ติดตั้งใหม่
npm install
```

### ปัญหา: TypeScript errors
**วิธีแก้:**
```bash
# ลบ cache
rm -rf .next

# Build ใหม่
npm run dev
```

## 📞 ติดต่อและสนับสนุน

หากมีคำถามหรือพบปัญหา:
1. ตรวจสอบ README.md
2. ดูที่โค้ดและคอมเมนต์ในไฟล์
3. แก้ไขและทดสอบเอง

## 🎉 สนุกกับการใช้งาน!

ระบบ Minimart POS ถูกออกแบบมาให้ใช้งานง่าย รวดเร็ว และมีประสิทธิภาพ
คุณสามารถปรับแต่งและพัฒนาต่อได้ตามความต้องการ

**Happy Coding! 💻🚀**
